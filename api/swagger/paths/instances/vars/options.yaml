get:
  summary: retrieves all var getters and setters for this instance
  description: |
    retrieves all var getters and setters for this instance
  tags:
    - "Vars"
  security:
    - bearerAuth: ["INSTANCE#ACCESS"]
  responses:
    '200':
      description: getters and setters
      content:
        application/json:
          schema:
            $ref: "../../../schemes/Vars.yaml#/Options"