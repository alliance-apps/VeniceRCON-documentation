post:
  summary: restarts the current round
  description: |
    restarts the current round
  tags:
    - "Maps"
  security:
    - bearerAuth: ["MAP#SWITCH"]
  responses:
    '200':
      description: map restarted successfull