post:
  summary: sends a message
  description: |
    sends a message to the player or his team/squad
  tags:
    - "Player"
  security:
    - bearerAuth: ["PLAYER#MESSAGE"]
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          properties:
            message:
              type: string
            subset:
              type: string
              enum:
                - player
                - squad
                - team
              nullable: true
            yell:
              type: boolean
              nullable: true
            yellDuration:
              type: number
              nullable: true
  responses:
    '200':
      description: message has been sent successfully