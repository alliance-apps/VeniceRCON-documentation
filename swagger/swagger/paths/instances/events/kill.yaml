get:
  summary: retrieves kills
  description: |
    retrieves a certain amount of kills from a specific date
    Required Scope: EVENT#KILL
  tags:
    - "Events"
  security:
    - bearerAuth: ["EVENT#KILL"]
  parameters:
    - in: query
      name: count
      schema:
        type: integer
      description: amount of messages to retrieve
    - in: query
      name: from
      schema:
        type: string
        format: date
      description: date to retrieve messages from
  responses:
    '200':
      description: object with variables
      content:
        application/json:
          schema:
            $ref: "../../../schemes/Kill.yaml#/Kills"