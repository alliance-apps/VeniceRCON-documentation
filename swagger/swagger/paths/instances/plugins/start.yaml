post:
  summary: starts the plugin
  description: |
    starts the plugin
  tags:
    - "Plugin"
  security:
    - bearerAuth: ["PLUGIN#MODIFY"]
  responses:
    '200':
      description: plugin has been started successfully