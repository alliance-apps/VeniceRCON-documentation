get:
  summary: retrieves a list of plugins which can be used
  description: |
    retrieves a list of plugins which can be used
  tags:
    - "Plugin"
  security:
    - bearerAuth: ["PLUGIN#ACCESS"]
  responses:
    '200':
      description: retrieves data about the current plugin
      content:
        application/json:
          schema:
            $ref: "../../../schemes/Plugin.yaml#/Plugin"